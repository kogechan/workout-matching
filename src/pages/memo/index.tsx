import { AddMemo } from '@/components/AddMemo';
import { MemoList } from '@/components/MemoList';

const Home = () => {
  return (
    <div>
      <AddMemo />
      <MemoList />
    </div>
  );
};

export default Home;
